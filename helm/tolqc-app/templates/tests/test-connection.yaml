apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tolqc-app.fullname" . }}-test-connection"
  labels:
    {{- include "tolqc-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tolqc-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
