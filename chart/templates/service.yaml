apiVersion: v1
kind: Service
metadata:
  name: {{ include "tolqc-app.fullname" . }}
  labels:
    {{- include "tolqc-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tolqc-app.selectorLabels" . | nindent 4 }}
